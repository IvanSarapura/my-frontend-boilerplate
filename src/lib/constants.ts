// Demo API origin for the example `posts` feature. Single source of truth:
// also feeds the `connect-src` CSP entry in src/proxy.ts.
export const DEMO_API_ORIGIN = 'https://jsonplaceholder.typicode.com';
